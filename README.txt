create template that is suitable for developing

* academic papers
* R packages -> no, use R package structure...
* empirical R projects

* rerunning make on save using R script ... hopefully ...


Requirements
============

On Windows

* tex distribution, e.g. MikTex
* R
* perl, e.g. Strawberry perl
* make, e.g. i) from MinGW + MSYS, ii) from Rtools